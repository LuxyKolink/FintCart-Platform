Square checkbox with optional inline label; relies on the browser's native check via `accent-color`.

```jsx
<Checkbox label="Acepto la política de tratamiento de datos (Ley 1581)" />
<Checkbox label="Recordarme" defaultChecked />
```
